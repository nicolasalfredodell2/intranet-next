import MainClientLayout from "@/components/main/MainClientLayout";

export default function MainLayout({ children }: { children: React.ReactNode }) {
  return <MainClientLayout>{children}</MainClientLayout>;
}
